# Design System

## Visual Direction

看山督学局使用“雾蓝学习桌”视觉世界。大面积雾蓝背景承载纸白功能面板，黑色超粗中文标题负责建立节奏，电光蓝只用于当前状态和主操作。刘看山的透明动画会跨越卡片边界，成为全平台的陪学线索。

## Tokens

- Fog `#DCEAF4`: 页面背景
- Paper `#FFFFFF`: 内容面板
- Ink `#101114`: 标题和主文字
- Electric blue `#2F6BFF`: 主操作、当前状态、焦点
- Navy `#173A78`: 深色辅助文字
- Ice `#EEF5FA`: 浅色内容容器
- Muted `#60738F`: 次级文字

卡片圆角为 12–16px，只使用有垂直位移的柔和阴影。胶囊仅用于筛选器、标签和状态。

## Typography

全站使用项目内置的 Noto Sans SC Variable。首屏标题使用 800–900 字重和紧凑字距，正文使用 400–650 字重。工作台的信息层级优先通过字号、字重和间距区分，不额外添加装饰线。

## Layout

- 公共页面使用最大 1480px 容器和非对称两列布局。
- 首页的核心是“继续当前课程”，课程探索居次。
- 课程广场以搜索和分类为入口，未开放课程使用明确状态标签。
- 学习工作台为真实三列任务结构：课程目录、学习主画布、AI 助教与督学工具。
- 820px 以下改为单列流；公共导航在窄屏改为等宽三项导航。

## Components

- `SiteHeader`: 全平台公共导航，当前页使用底部蓝色短线。
- `LiuKanshan`: 根据督学状态切换正式透明 GIF，禁止用 emoji 占位。
- `CameraPanel`: 开启前展示隐私说明，开启后展示本地检测状态和误判纠正。
- `KanshanPunch`: 刘看山从右侧陪学框跃向屏幕中心，再播放透明拳击动画和自带音效；可点击“我回来了”或按 Esc 关闭。
- `Icon`: 24px 单色线性 SVG 图标，保持统一线宽和端点。

## Motion

正常陪学动画来自用户提供的透明 GIF。拳击是页面唯一的强动效：角色从右侧陪学框跃向屏幕中心，播放透明 WebM 后返回原位，用于明确反馈“持续分心”状态。开启 `prefers-reduced-motion` 时跳跃降低为淡入反馈，但保留文字状态。

## Content Rules

- 当前只有一门完整可用课程，不伪造其他课程的开课状态。
- 不使用虚构评分、学习人数、教师资历或证书。
- 刘看山文案温和、具体、可行动，不羞辱、不威胁。
- 知乎内容保留作者与原文链接，AI 总结必须单独标识。
